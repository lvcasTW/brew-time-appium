[pytest]
addopts = -ra
testpaths = tests
pythonpath = .
markers =
    level1: fluxos básicos (login, abas)
    level2: scroll, long press, swipe, xpath de exemplo
    level3: webview, paralelo, deep link
    android: requer sessão Android (padrão da suíte)
