# API — Karate DSL

Maven + JUnit 5. Requer JDK 17+ e Maven.

```bash
cd automation/api-karate
mvn test
```

`baseUrl` padrão vem de `karate-config.js` (sobrescreva com `-Dkarate.env=staging` se criar perfis).
